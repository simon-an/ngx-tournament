import { TeamColorDirective } from './team-color.directive';

describe('TeamColorDirective', () => {
  it('should create an instance', () => {
    const directive = new TeamColorDirective(null);
    expect(directive).toBeTruthy();
  });
});
